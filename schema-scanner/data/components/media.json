{
  "slug": "/_schema/media",
  "components": {
    "meta": {
      "label": "media",
      "content": "Attached imagery, audio, or video. For long-form text use `post`.",
      "tags": ["schema", "component"]
    },
    "schema": {
      "componentName": "media",
      "fields": {
        "kind":    { "type": "string",   "description": "photo | video | audio | thumbnail | other" },
        "urls":    { "type": "string[]", "description": "One or more URLs to the media asset" },
        "caption": { "type": "string",   "description": "Short caption or alt text" },
        "credit":  { "type": "string",   "description": "Attribution / photographer / source" }
      }
    }
  },
  "_provenance": [{ "action": "new", "rationale": "Separates media URLs from generic content (post.body) and avoids overloading meta.depiction for multi-asset cases." }]
}
