{
  "slug": "/_schema/schedule",
  "components": {
    "meta": {
      "label": "schedule",
      "content": "Planned vs. actual timing for journeys, shipments, and operations. Distinct from `event` (a thing that happened) — schedule is the intent and the slip from intent.",
      "tags": [
        "schema",
        "component"
      ]
    },
    "schema": {
      "componentName": "schedule",
      "fields": {
        "planned_arrival": {
          "type": "string",
          "description": "ISO 8601 planned arrival"
        },
        "actual_arrival": {
          "type": "string",
          "description": "ISO 8601 actual arrival"
        },
        "planned_departure": {
          "type": "string",
          "description": "ISO 8601 planned departure"
        },
        "actual_departure": {
          "type": "string",
          "description": "ISO 8601 actual departure"
        },
        "destination": {
          "type": "string",
          "description": "Slug of destination entity (location / region / facility)"
        },
        "recurrence": {
          "type": "string",
          "description": "RRULE or cron-like recurrence string"
        }
      }
    }
  }
}
