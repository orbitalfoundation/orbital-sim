{
  "slug": "/_schema/vessel",
  "components": {
    "meta": {
      "label": "vessel",
      "content": "Ship, boat, or watercraft characteristics. Combine with `physical`, `condition`, `cargo`, `schedule`, `relation` (ownership, captain).",
      "tags": [
        "schema",
        "component"
      ]
    },
    "schema": {
      "componentName": "vessel",
      "fields": {
        "vessel_type": {
          "type": "string",
          "description": "container_ship | tanker | bulk_carrier | yacht | fishing_boat | cutter | tug | sailboat | ..."
        },
        "propulsion": {
          "type": "string",
          "description": "diesel | steam | sail | nuclear | electric | auxiliary | ..."
        },
        "registration": {
          "type": "string",
          "description": "Registration number / IMO / call sign"
        },
        "flag": {
          "type": "string",
          "description": "Country of registration (ISO code)"
        },
        "draft_m": {
          "type": "number",
          "description": "Draft (depth below waterline) in metres"
        }
      }
    }
  }
}
