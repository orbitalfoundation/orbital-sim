{
  "slug": "/_schema/cargo",
  "components": {
    "meta": {
      "label": "cargo",
      "content": "A consignment being transported. Pairs with `quantity` (how much) and `relation` (whose / to whom).",
      "tags": [
        "schema",
        "component"
      ]
    },
    "schema": {
      "componentName": "cargo",
      "fields": {
        "state": {
          "type": "string",
          "description": "frozen | refrigerated | dry | liquid | hazardous | live | ..."
        },
        "destination": {
          "type": "string",
          "description": "Slug of destination entity"
        },
        "manifest": {
          "type": "string",
          "description": "Reference to manifest content"
        }
      }
    }
  },
  "_provenance": [
    {
      "action": "refactor",
      "rationale": "Removed `quantity` and `unit` (now in the `quantity` component)."
    }
  ]
}
